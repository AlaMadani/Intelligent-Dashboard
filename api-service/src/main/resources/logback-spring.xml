<configuration>
    <springProperty name="applicationName" source="spring.application.name" defaultValue="api-service"/>
    <springProperty name="logstashDestination" source="logging.logstash.destination" defaultValue="localhost:5000"/>

    <!-- Human-readable console logs for local development. -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - [%5p] - %m%n</pattern>
        </encoder>
    </appender>

    <!-- Structured logs forwarded to Logstash for centralized analysis. -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>${logstashDestination}</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"app_name":"${applicationName}"}</customFields>
        </encoder>
    </appender>

    <!-- Publish every INFO+ log to both local console output and Logstash. -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="LOGSTASH" />
    </root>
</configuration>
